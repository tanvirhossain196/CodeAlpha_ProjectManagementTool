services:
  postgres:
    image: postgres:16-alpine
    container_name: shilposetu-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: shilposetu
    ports:
      - "55434:5432"
    volumes:
      - shilposetu_pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d shilposetu"]
      interval: 5s
      timeout: 5s
      retries: 10
volumes:
  shilposetu_pgdata:
